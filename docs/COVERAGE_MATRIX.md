# zioshade opcode/capability coverage matrix

> Generated by `tools/coverage_matrix.sh` (regenerable -- do not hand-edit). Methodology caveats in the script header.
> Universe = the `Op` enum in `src/spirv.zig` (**252** opcodes zioshade models). The SPIR-V core spec is larger (~310+); zioshade targets the shader-relevant subset.

## Per-backend static Op-opcode handling

| Backend | Statically handled | / modelled |
|---|---:|---:|
| HLSL | 240 | 252 |
| MSL  | 220  | 252 |
| WGSL | 222 | 252 |
| GLSL | 218 | 252 |

Handled by **all 4**: 222.  Handled by **>=1**: 246.
"Handled" is a lower bound (distinct Op members the backend + shared-common code reference); real coverage is >= this. Authoritative corpus proof: `tools/check_unhandled.zig` reports **0 unhandled op** across tests/{glslang-430,spirv-cross,ghostty}.

## Empirical reach (56 real SPIR-V fixtures in tests/spirv_bins)

- **94** distinct opcodes exercised; **93** modelled by zioshade.
- Exercised but NOT modelled: SourceExtension  (1 total -- trivial debug/metadata opcodes only; no shader-semantic opcode in the fixtures is unmodelled; "Image" maps to enum member OpImage).
- **12** distinct capabilities exercised: FragmentBarycentricKHR FragmentShaderPixelInterlockEXT FragmentShaderSampleInterlockEXT ImageBuffer ImageQuery Int64 MeshShadingEXT SampleMaskPostDepthCoverage SampleRateShading SampledBuffer Shader ShaderClockKHR 

## Modelled but not statically handled by any backend (6)

Capability ExtInstImport Extension MemoryModel TensorQuerySizeARM TensorReadARM 

Sub-group: **module metadata** (Capability, Extension, ExtInstImport, MemoryModel) is processed structurally -- NOT a gap. The rest (2) are the real deferred areas: TensorQuerySizeARM TensorReadARM 

## GLSLstd450 extended instructions

Universe: **81**. Per-backend static counts are omitted (GLSL/MSL/WGSL dispatch ExtInst by name, not enum arm -- a static count would misread 0). Corpus proof: `check_unhandled.zig` reports **0 unhandled std450**.

## Scope statement this matrix supports

zioshade models **252** Op opcodes; the real-fixture corpus exercises **94** distinct opcodes, **93** of them modelled and handled (only debug-metadata unmodelled); the conformance corpora produce **0 unhandled** opcodes/std450. The correctness scope is therefore bounded and measurable, not implicit. Known deferred opcodes are enumerated above and each is a named honest-error, never silent-wrong.
