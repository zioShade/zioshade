# Handoff — glslpp → wintty Integration

> Auto-generated session handoff. Read this first in the next session.

## Current State

### glslpp repo (`C:/Users/Alessandro/CODE/OSS/glslpp`)
- **Branch**: `main`
- **Last commit**: `277afb2` docs: update README
- **Zig version**: 0.15.2 (pinned via `.mise.toml` + `.zig-version`)
- **Test status**: 128/128 HLSL tests pass, 0 fail, 2 leaked (pre-existing, from wintty shader tests)
- **Build status**: Clean

### wintty repo (`C:/Users/Alessandro/CODE/OSS/wintty`)
- **Branch**: `feat/glslpp-integration`
- **Last commit**: `36262a8` docs: update shader comments
- **Build status**: Clean (`zig build -Doptimize=ReleaseSafe` succeeds)
- **Test status**: Pre-existing failure in `src/termio/Exec.zig` (2 compile errors, unrelated to glslpp)

## What Was Completed

1. **Zig 0.15.2 pinned** via mise in glslpp repo
2. **wintty CRT shader passes end-to-end**: GLSL → SPIR-V (1691 words) → HLSL (5800 bytes)
3. **DXC validation**: 0 errors, 0 warnings, 5932 bytes DXIL output
4. **Binding remap verified**: `binding=1` → `register(b0)` for DX12 root signatures
5. **`compileGlslToHlsl()` added** — one-shot API for wintty
6. **glslpp wired into wintty** as a dependency, `shadertoy.zig` uses it for HLSL path
7. **8MB stack thread skipped** for HLSL target (glslpp is pure Zig, no C++ ABI issues)
8. **Benchmark**: ~3.6ms average per shader (well under any budget)
9. **128/128 HLSL tests pass** in glslpp

## What's Next (Deferred Items, Priority Order)

### P0: Fix focus shader (WIN2)
- **File**: `tests/hlsl_tests.zig` — test `WIN2` currently only checks that compilation doesn't crash
- **Problem**: `mainImage` function body gets entirely dropped. Output is just `float4 main() { return; }`
- **Root cause**: NOT a bool/|| issue (simple bool tests pass). The function body is dropped during semantic analysis or DCE. The shader uses `texture2D(iChannel0, uv)`, `iFocus > 0` (int comparison), nested if/else with complex expressions.
- **Debugging approach**: Add debug prints in `src/semantic.zig` around function analysis (`analyzeFunction` ~line 1325), check if the issue is in parsing, semantic analysis, or the DCE pass in `src/codegen.zig`
- **Related files**: `src/semantic.zig`, `src/codegen.zig`, `src/parser.zig`
- **The shader**: `tests/wintty/test_focus.glsl` (prepended with `tests/wintty/shadertoy_prefix.glsl`)

### P1: Remove shader_wrapper.dll infrastructure
- **Blocked by**: Nothing technically, but glslang+spirv-cross are still needed for MSL/GLSL
- **Files to remove**: `pkg/glslang/shader_wrapper.cpp`, `pkg/glslang/wrapper.zig`
- **Must update**: `pkg/glslang/main.zig` (remove `pub const wrapper = @import("wrapper.zig")`)
- **Risk**: Cannot delete `pkg/glslang/msvc_build/` — it's generated by `build_msvc.bat` and needed for glslang. The .bat requires a VS Developer Shell to rebuild.

### P2: MSL backend (for macOS Metal renderer)
- **File to create**: `src/spirv_to_msl.zig` (mirroring `src/spirv_to_hlsl.zig` structure)
- **Then**: Update `shadertoy.zig` to use glslpp for MSL path too, remove spirv-cross dependency
- **This is the biggest remaining item** — likely 1-2 weeks of work

### P3: GLSL backend (for OpenGL renderer)
- **File to create**: `src/spirv_to_glsl.zig`
- **Then**: Remove glslang dependency entirely

### P4: Performance optimization
- **Current benchmark**: ~3.6ms avg — no optimization needed
- **If needed later**: Profile with timing in `compileToSPIRV` to find bottlenecks (parser, semantic, codegen)
- **Common targets**: ArrayList pre-allocation, string interning in spirv_to_hlsl.zig

### P5: Memory leak cleanup
- **2 leaks in HLSL tests** from wintty shader compilation (semantic analyzer owned_name allocations)
- **Fix location**: `src/semantic.zig` line ~1190 (collectTopLevel dupe)
- **Also**: Pre-existing leaks in unit tests (2 failures: codegen for-loop test, diagnostics test)

## Key Files to Read First

1. `docs/superpowers/plans/2026-05-09-wintty-integration.md` — the master plan (fully written out)
2. `src/root.zig` — public API including `compileGlslToHlsl()`
3. `src/spirv_to_hlsl.zig` — SPIR-V → HLSL cross-compiler (~1700 lines)
4. `tests/hlsl_tests.zig` — 128 tests including wintty integration tests (WIN1, WIN2, WIN3)
5. `C:/Users/Alessandro/CODE/OSS/wintty/src/renderer/shadertoy.zig` — the integration point

## Open Questions / Blockers

1. **Focus shader body drop** — needs investigation. Could be a parser issue (function body not parsed), semantic issue (body analyzed but IR not generated), or DCE issue (IR generated but optimized away)
2. **MSL/GLSL backends** — significant new development, no work started
3. **wintty `Exec.zig` test failures** — pre-existing, unrelated to glslpp. 2 compile errors about argument count mismatch
4. **`build_msvc.bat` fragility** — requires VS Developer Shell. If msvc_build/ is lost, it's hard to regenerate
