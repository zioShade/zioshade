# CTS ingestion sweep crash baseline (tests/cts/graphicsfuzz corpus).
# One line per backend: <backend> <crash_count>. tools/cts_ingestion_sweep.sh FAILS if a
# backend's CURRENT crash count EXCEEDS the value here (a new crash on valid input is a
# mandate violation -- silent wrong). invalid-output and honest-error counts are non-gating
# (breadth) and intentionally NOT tracked here. When a crash is fixed, lower the count.
#
# FILE-LEVEL since 2026-08-15: each backend's exact crash files are listed in a
# `crash-files:<backend>` section below. Any file that crashes NOW but is not listed is a
# NEW crash and fails the gate; a listed file that no longer crashes prints a prune
# reminder (a fix + a different new crash at the same count can no longer pass silently).
# The <backend> <count> rows below are informational summaries kept for quick reading.
#
# glsl 0 = graphicsfuzz_032.spv crash FIXED (emitWhileLoop depth-bound -> honest-error, PR
#          #522). It now refuses loudly instead of stack-exhausting. The deeper loop-
#          lowering fix (handle the cyclic/self-referential LoopMerge correctly rather than
#          refusing) is a follow-up. The invalid-output breadth this leg opened with
#          (54 at the 2026-08-04 first measurement) has since been driven to 0 (PRs
#          #574/#575/#579/#584/#589/#591/#593/#596/#611/#613); 2026-08-15 sweep:
#          ok=62 invalid-output=0 honest-error=26.
# msl 0 = the FIRST run of this leg (2026-08-04) found graphicsfuzz_022.spv and
#         graphicsfuzz_082.spv CRASHING (SIGSEGV, rc=139, no diagnostic):
#         emitWhileLoopMSL and emitBlock recursed into each other on the SAME loop
#         header (lldb, pre-fix line numbers: alternating spirv_to_msl.zig:7237 / :7442
#         frames, loop_idx=75, merge 62, cont 60, block 68) until the stack guard page
#         was hit. Both inputs pass spirv-val, so this was a crash on VALID input. Fixed
#         by porting the GLSL max_emit_while_depth guard (PR #522) to the MSL backend:
#         it now refuses loudly (CrossCompileUnsupported), moving both files from CRASH
#         to honest-error (MSL honest-error 19 -> 21). Lowering these loops correctly
#         instead of refusing is the same follow-up as glsl/gf032. The invalid-output
#         breadth this leg opened with (47) reached 0 in the merged-PR record (#566,
#         #569); the 2026-08-15 local sweep (swiftc oracle under the CommandLineTools
#         SDK, no Xcode) reported ok=66 honest-error=21 and one rejection that was an
#         MTLLibrary XPC_ERROR_CONNECTION_INTERRUPTED (oracle service failure, not a
#         source rejection) -- the macOS CI job with the real Metal toolchain is the
#         authoritative leg.
# hlsl 0 = no crash on this corpus at the row's first measurement (2026-08-04, DXC
#          1.9.0.5191; ok=20 invalid-output=46 honest-error=22). The invalid-output
#          breadth reached 0 in the merged-PR record (#557-#563, #567, #572); this leg
#          needs DXC and is enforced by the Windows CI job, not locally.
# wgsl 0 = 2026-08-15 sweep: ok=61 candidate-invalid=2 honest-error=25. The two
#          candidate-invalids are the known WGSL residue (naga rejects; no second
#          WGSL-producing oracle exists to discriminate real-bug vs oracle-strictness).
#
# The msl and hlsl legs only RUN where their compiler exists (macOS Metal, DXC), so on a
# Linux runner they skip and report 0, which can never exceed the baseline. The macos and
# windows CI jobs are what actually enforce these two rows.
glsl 0
wgsl 0
msl 0
hlsl 0

# File-level crash baselines (one basename per line under each marker; empty = none).
crash-files:glsl
crash-files:wgsl
crash-files:msl
crash-files:hlsl
