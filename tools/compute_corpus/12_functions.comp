#version 450
layout(local_size_x = 64) in;
layout(std430, binding = 0) readonly buffer InBuf { float inData[]; };
layout(std430, binding = 1) writeonly buffer OutBuf { float outData[]; };
float g(float a, float b) { return a * b + a - b; }
float f(float t) { return g(t, t + 1.0) - g(t * 0.5, 2.0); }
void main() {
    uint i = gl_GlobalInvocationID.x;
    float x = inData[i];
    float r = f(x) + f(-x) + fma(x, 3.0, 1.0) + g(x, x);
    outData[i] = r;
}
